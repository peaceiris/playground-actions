## playground-actions

The latest release v1.0.23

```sh
# ./release.sh minor
./release.sh patch

# Marge Pull Request

git checkout master
git pull origin master
git fetch
git branch -D release-patch
```
hoge
